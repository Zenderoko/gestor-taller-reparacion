{
  "name": "gestor-taller",
  "version": "1.0.0",
  "private": true,
  "description": "Gestor de Taller de Reparaciones",
  "scripts": {
    "setup": "npm install --prefix backend && npx --prefix backend prisma db push && node backend/prisma/seed.js && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
